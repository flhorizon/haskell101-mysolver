Handle non-0 right hand ( Expr = \{0} )
	and special cases ( 0 = 0 -> roots = |R )
			  ( aX^0 = 0  -> Absurd ) -- OK
Echo Reduced form. -- EZ
Echo order. -- EZ
Echo "won't solve" if not canonical. -- almost
Echo Discriminant sign --"if relevant"-- TODO; trivial

V - Finish directory structure (& build) refactoring
V - Finish parser integration ( see prototype Mk II ) 
  - Check feature consistency vs specs
  + Use Monads to log error and handle failures. Transformers ? 
  ++ More detailed parser error messages
