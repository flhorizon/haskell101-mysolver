Handle non-0 right hand ( Expr = \{0} )
	and special cases ( 0 = 0 -> roots = |R )
			  ( aX^0 = 0  -> Absurd ) -- OK
Echo Reduced form. -- EZ
Echo order. -- EZ
Echo "won't solve" if not canonical. -- almost
Echo Discriminant sign --"if relevant"-- TODO; trivial
PARSER FFS. Lol.

Next : Polynomials as a map of lists (int i : [(cn :*^: i)]) ; assert with lookupLT 0 / lookupGT 2
Next++ : embed with Writer to log tests and progress
